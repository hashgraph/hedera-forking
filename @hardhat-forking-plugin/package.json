{
  "name": "@hashgraph/hardhat-forking-plugin",
  "version": "1.0.7",
  "description": "Hashgraph Hardhat TypeScript plugin",
  "repository": "github:hashgraph/hardhat-forking-plugin",
  "author": "2024 Hedera Hashgraph, LLC",
  "license": "MIT",
  "main": "src/index.js",
  "keywords": [
    "ethereum",
    "smart-contracts",
    "hardhat",
    "hardhat-plugin"
  ],
  "scripts": {
    "test": "mocha --exit --recursive",
    "build": "rm -rf out/* && cp -R ../../out/HtsSystemContract.sol out && cp ../../test/lib/HIP719.bytecode out",
    "prepublishOnly": "npm run build"
  },
  "files": [
    "src/",
    "out/",
    "README.md"
  ],
  "peerDependencies": {
    "ethers": "^6.11.2",
    "hardhat": "^2.0.0"
  },
  "devDependencies": {
    "@types/sinon": "^17.0.3",
    "prettier": "^3.3.3",
    "sinon": "^19.0.2"
  }
}
